# Control computer with gestures

## How to use

* Point thumb up or down to scroll up or down
* Move thumb around to move cursor
* Touch index with thumb to click

## Dependencies
* If you are using conda, just install the dependencies with the provided requirements.yml.
* Otherwise, create a virtual environment with pip and run "pip install opencv-python mediapipe pyautogui"